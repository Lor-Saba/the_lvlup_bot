
*, *:before, *:after {
    box-sizing: inherit;
    user-select: none;
}

html {
    font-family: 'Nunito', sans-serif;
    font-size: 16px;
    margin: 0;
    box-sizing: border-box;
}

body {
    margin: 0;
    box-sizing: border-box;
}